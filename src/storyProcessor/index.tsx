const GetSection = (story: Story, path: number[]) => {
    const activeSegment: Story | StorySegment;
    
    path.reduce((returnedSegment))

    const returnedSegment;
    return returnedSegment;
};

export default GetSection;
